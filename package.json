{
  "name": "paper-website-template",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A dependency-free academic project page with HKUST and SUSTech branding.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node scripts/build.mjs --check && node scripts/check.mjs",
    "start": "python3 -m http.server 8000 --bind 127.0.0.1"
  },
  "engines": { "node": ">=20" }
}
